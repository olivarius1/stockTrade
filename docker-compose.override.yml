# 开发环境说明:
# 开发模式不需要此文件。开发步骤:
#   1. docker-compose up -d          (启动 PG + Redis 基础设施)
#   2. cd backend && python init_db.py
#   3. uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
#
# 生产部署:
#   docker-compose --profile prod up -d --build
